name: Deploy Angular App to SiteGround

on:
  push:
    branches:
      - dev
  pull_request:
    branches:
      - dev

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # Clonar el repositorio en la máquina del runner
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Pruebas Consola
        run: |
          ls -la
    